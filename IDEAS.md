
"Split" button on front port detail page to split a port into multiple ports. This is useful when a port has multiple positions and you want to manage them separately.
"Merge" button on front port detail page to merge multiple ports into a single port. This is useful when you have multiple ports that are actually the same physical port and you want to manage them as one.